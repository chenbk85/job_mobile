; 주석 앞에 공백이 있으면 에러남


[exe]

; default run env folder.
start_dir = ".."

; is run multi excute
multi_run = 0

; default app_xml name. if not notified use app name.
default_app_xml = ""

; is run TestUnit
run_unit_test = 0

; is print string index
MAKE_XMLID = 0

; run env languge locale
locale = "kor"

; log file save folder
log_file_path = "c:"

